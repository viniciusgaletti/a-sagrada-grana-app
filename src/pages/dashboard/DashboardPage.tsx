export default function DashboardPage() {
  return (
    <div className="p-6">
      <h1 className="font-serif text-3xl" style={{ color: 'var(--foreground)' }}>
        Dashboard
      </h1>
      <p className="mt-2 text-sm" style={{ color: 'var(--muted-foreground)' }}>
        Gastos por categoria — em desenvolvimento.
      </p>
    </div>
  )
}
